{
  "mcpServers": {
    "netbox": {
      "command": "/home/netops/ai/scripts/netbox-mcp-launch.sh"
    },
    "grafana": {
      "command": "/opt/mcp-grafana/mcp-grafana",
      "env": {
        "GRAFANA_URL": "${GRAFANA_URL}",
        "GRAFANA_API_KEY": "${GRAFANA_API_KEY}"
      }
    }
  }
}
